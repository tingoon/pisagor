# Vue Charts

Chart primitives for `@pisagor/vue-charts`. Stories under `Charts/…` in Vue Storybook.

```bash
turbo type-check --filter=@pisagor/vue-charts
turbo dev --filter=vue-storybook
```

Import from `@pisagor/vue-charts`. Do not author `tv()` here — use `@pisagor/recipes/chart`.
