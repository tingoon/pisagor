# Migrating to Pisagor Astro

- Import from `@pisagor/astro` / `@pisagor/astro/<name>` — not `@/components/ui`.
- Do not assume Radix/shadcn or full React/Vue compound APIs.
- Interactive overlays may require a React/Vue island + `react` / `vue`.

