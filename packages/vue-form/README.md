# @pisagor/vue-form

Form fields and TanStack Form helpers for Vue.

```ts
import { TextField } from "@pisagor/vue-form";
import { useAppForm } from "@pisagor/vue-form/tanstack";
```

Peer: `@tanstack/vue-form` ^1, `vue` ^3.5. Fields compose `@pisagor/vue`.
