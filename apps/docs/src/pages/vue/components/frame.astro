---
import {
  Default,
  sources as examples,
  imports,
  SeparatedPanels,
  WithFormControls,
} from "@pisagor/vue/frame/examples";
import CodeBlock from "../../../components/docs/code-block.astro";
import ComponentHeader from "../../../components/docs/component-header.astro";
import ComponentPreview from "../../../components/docs/component-preview.astro";
import Installation from "../../../components/docs/installation.astro";
import PropsTable from "../../../components/docs/props-table.astro";
import DocsLayout from "../../../layouts/docs-layout.astro";
import { frameProps } from "../../../lib/props/frame";

const headings = [
  { depth: 2, slug: "usage", text: "Usage" },
  { depth: 3, slug: "separated-panels", text: "Separated Panels" },
  { depth: 3, slug: "with-form-controls", text: "With Form Controls" },
  { depth: 3, slug: "default", text: "Default" },
  { depth: 2, slug: "installation", text: "Installation" },
  { depth: 2, slug: "props", text: "Props" },
];

---

<DocsLayout
  description="Embeds external content in a framed viewport with a consistent chrome around it."
  framework="vue"
  headings={headings}
  title="Frame"
>

  <ComponentHeader framework="vue" id="frame" />


  <h2 class="mb-3 text-2xl font-semibold tracking-tight" id="usage">Usage</h2>
  <p class="mb-4 text-muted-foreground">
    Each example matches a Storybook story, in the same order.
  </p>

  <CodeBlock class="mb-8" code={imports} lang="ts" title="Import" />

  <h3 class="mb-3 mt-10 text-xl font-semibold tracking-tight" id="separated-panels">Separated Panels</h3>
  <ComponentPreview code={examples.SeparatedPanels} language="vue" title="Separated Panels">
    <SeparatedPanels client:only="vue" />
  </ComponentPreview>
<h3 class="mb-3 mt-10 text-xl font-semibold tracking-tight" id="with-form-controls">With Form Controls</h3>
  <ComponentPreview code={examples.WithFormControls} language="vue" title="With Form Controls">
    <WithFormControls client:only="vue" />
  </ComponentPreview>
<h3 class="mb-3 mt-10 text-xl font-semibold tracking-tight" id="default">Default</h3>
  <ComponentPreview code={examples.Default} language="vue" title="Default">
    <Default client:only="vue" />
  </ComponentPreview>

  <h2 class="mb-3 mt-12 text-2xl font-semibold tracking-tight" id="installation">Installation</h2>
  <Installation class="mb-8" packages="@pisagor/vue" />

  <h2 class="mb-3 text-2xl font-semibold tracking-tight" id="props">Props</h2>
  <p class="mb-4 text-muted-foreground">
    Own props from <code>@pisagor/props/frame</code>. Native HTML attributes are also supported and omitted from this table.
  </p>
  <PropsTable rows={frameProps} />
</DocsLayout>
